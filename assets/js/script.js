
// on active JQuery
$(document).ready(function() {

    // activation dropdown
    $(".dropdown-trigger").dropdown();

    // activation parallax
    $('.parallax').parallax();

    // activation sidenav-trigger
    $('.sidenav').sidenav();

    // activation modal-trigger
    $('.modal').modal();
            
     
});